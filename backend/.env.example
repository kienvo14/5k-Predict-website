# Copy this file to `.env` for local dev (never committed).
# Leave DATABASE_URL commented out to use local SQLite.
# Uncomment + fill in ONLY if you want to point local dev at a Postgres server.

# DATABASE_URL=postgresql://user:password@host:5432/dbname
